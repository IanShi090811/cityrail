# v147 新手教程

- 新增 Apple 风格的新手教程浮层，覆盖账号支付、城市选择、建线建站、站点功能区、换乘、线路配置、模拟运行、OD 导航、控制中心、快慢车、越行站、ATS 调度、存档设置与交互稳定等功能。
- 新注册用户自动弹出最多 3 次；用户可勾选“以后不再自动弹出”。
- 设置面板新增“新手教程 / 开启新手教程”入口，可随时重新打开。
- 教程只读状态与 UI，不接管既有业务按钮，不改动支付、列车、客流、控制中心、ATS、地图等正常逻辑。
- 增加 `cityrailTutorialReport()` 和 `CityRailTutorial.start()` 诊断入口。
