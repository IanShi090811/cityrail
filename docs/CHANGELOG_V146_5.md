# CityRail v146.5

- 控制中心“全网总客流”统一以“万”为单位显示，保留一位小数，例如 `12.3万`，不再显示具体整数。
- 控制中心顶部总客流 KPI 与“全网客流概览”中的总客流使用同一格式。
- 控制中心线路表“快慢车”列只显示 `已开启` 或 `未开启`，不再显示快/慢车数量、越行说明等附加内容。
- 增加控制中心显示归一化保护，防止旧渲染在刷新后把上述两个显示规则改回去。
- 已运行 `npm run selfcheck`。
